rules_version = '2';

// 認証なしの少人数内部ツールのため、URLを知っていれば誰でも読み書き可能とする（意図的な設計）
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
